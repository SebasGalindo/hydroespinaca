import { View, Text } from 'react-native';

export function FormContainer(): JSX.Element {
  return (
    <View style={{ padding: 16 }}>
      <Text>Soy un componente FormContainer</Text>
    </View>
  );
}